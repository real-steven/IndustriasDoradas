<UserControl x:Class="IndustriasDoradas.Desktop.Presentation.Views.OperationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:IndustriasDoradas.Desktop.Presentation.Views"
             AutomationProperties.Name="Modo Operación">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="38,30,38,36" MaxWidth="900" HorizontalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Foreground="#8A5B0A"
                               FontSize="12"
                               FontWeight="Bold"
                               Text="MODO OPERACIÓN" />
                    <TextBlock Margin="0,7,0,0"
                               Foreground="{StaticResource Ink900Brush}"
                               FontSize="29"
                               FontWeight="Bold"
                               Text="Control de producción local" />
                </StackPanel>
                <Button Grid.Column="1"
                        MinWidth="100"
                        VerticalAlignment="Center"
                        AutomationProperties.Name="Actualizar estado operativo"
                        Command="{Binding RefreshCommand}"
                        Content="Actualizar" />
            </Grid>

            <Border Grid.Row="1"
                    Margin="0,20,0,20"
                    Padding="18,12"
                    Background="#EAF7EF"
                    CornerRadius="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Foreground="#176B3A"
                               FontSize="15"
                               FontWeight="Bold"
                               Text="{Binding LocalStorageStatus}" />
                    <TextBlock Grid.Column="1"
                               Margin="24,0,0,0"
                               Foreground="#344054"
                               FontSize="14"
                               Text="{Binding PendingStatus}" />
                </Grid>
            </Border>

            <views:OperationLinePanel Grid.Row="2" />

            <Border Grid.Row="3"
                    Margin="0,18,0,0"
                    Padding="18,14"
                    Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="10">
                <TextBlock AutomationProperties.LiveSetting="Polite"
                           Foreground="{StaticResource Ink700Brush}"
                           FontSize="15"
                           Text="{Binding LastResult}"
                           TextWrapping="Wrap" />
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
